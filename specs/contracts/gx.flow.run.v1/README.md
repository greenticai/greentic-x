# gx.flow.run.v1

Core Greentic-X flow run state spec.

This is a forward-looking run envelope that later GX executor work should
produce. It is added now so downstream repos can align on the intended shape.
