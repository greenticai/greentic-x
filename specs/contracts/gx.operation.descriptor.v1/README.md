# gx.operation.descriptor.v1

Core Greentic-X operation descriptor spec.

This spec generalizes the current `OperationManifest` plus the shared operation
call/result envelopes introduced in `PR-GX-01`.
