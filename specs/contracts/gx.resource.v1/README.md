# gx.resource.v1

Core Greentic-X resource envelope spec.

This spec standardizes how generic resources are represented across contracts,
runtime APIs, catalogs, and downstream repos.

Contents:

- `manifest.json`: spec descriptor
- `schema.json`: JSON Schema for the resource envelope
- `examples/resource.example.json`: example resource document
