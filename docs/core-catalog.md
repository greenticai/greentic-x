# Core Catalog

The `catalog/core/` directory seeds a minimal reusable GX catalog.

It currently includes:

- contract/spec references
- generic resolver entries
- generic operation entries
- neutral view families
- forward-looking flow template descriptors

This catalog is intentionally small. Its purpose is to prove the spec layer is
usable without locking the repo into a broad domain vocabulary too early.
