Mirrored contract assets from the repo-level `contracts/` directory.

Each subdirectory contains the current checked-in reference contract manifest,
schemas, examples, and README for that contract family.
