Runtime-specific hooks, templates, and policy assets go here.

Current reference intent:

- `contract.validate`
- `op.execute`

Those hook names match the illustrative `applies_to.op_names` list in the pack
extension offer. They do not yet correspond to a production runtime component.
