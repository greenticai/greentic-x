Runtime-capability reference assets live here.

The current pack keeps placeholder schemas and examples plus notes about the
runtime hook points that the repo-level runtime may eventually expose through
real components.
