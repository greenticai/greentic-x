# Greentic-X Runtime Capability Reference Pack

Runtime capability scaffold.

Capability ID: `greentic.cap.runtime.execution.v1`
Component: `runtime-provider`
Operation: `evaluate`

This pack is the initial wizard-backed runtime capability example for this
repo. It carries a placeholder component plus example runtime input/output
schemas and one capability offer in `pack.yaml`.

The offer is intentionally illustrative rather than production-ready; the
component payload is still the minimal scaffold generated by the wizard.
